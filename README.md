# Supivaa Frontend

Supivaa’s frontend is a modern, responsive marketing site built with **React + TypeScript** and styled using **TailwindCSS**. It integrates seamlessly with backend APIs (Laravel/PHP) to deliver scalable, secure, and maintainable content management.

---

## ✨ Features

- **Responsive Design**  
  Fully optimized for desktop, tablet, and mobile with progressive media queries.

- **Modern Tech Stack**  
  - React + TypeScript for robust, type‑safe components  
  - Vite for fast builds and hot‑reload development  
  - TailwindCSS for utility‑first styling  
  - Filament/Livewire integration for admin panel (via backend)

- **Reusable Components**  
  Clean, modular structure for sections like Header, Footer, About, Values, Team, Partners, etc.

- **Accessibility & Performance**  
  Semantic HTML, ARIA attributes, lazy‑loaded images, optimized CSS for fast rendering.

- **Animations & Interactivity**  
  Subtle hover effects, scroll‑in animations, and interactive carousels.

---

## 🚀 Performance Highlights

- **Vite bundling** → lightning‑fast dev server and optimized production builds  
- **Code splitting** → only load what’s needed per route  
- **Image optimization** → responsive sizes, `object-fit`, and lazy loading  
- **CSS optimization** → Tailwind purge removes unused styles  
- **Accessibility checks** → keyboard navigation and screen reader support

---

## 🛠️ Getting Started

### Prerequisites
- Node.js (>= 18.x recommended)
- npm or yarn
- Git

### Installation
Clone the repository and install dependencies:

```bash
git clone https://github.com/Alex-gikungu/Supivaa_frontend.git
cd Supivaa_frontend
npm install
